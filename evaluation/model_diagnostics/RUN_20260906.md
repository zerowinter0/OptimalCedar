# Offline run handoff

Container: `optimalcedar-torch201-dev`.
Runner PID in container: `2493895` (PID may be reused after completion).

Host output directory:
`/home/xieruiyang/OptimalCedar/outputs/model_diagnostics/controlled_20260906_v2`

Container output directory:
`/workspace/OptimalCedar/outputs/model_diagnostics/controlled_20260906_v2`

- `STATUS`: RUNNING phase/case, COMPLETE, or FAILED with traceback.
- `runner.log`: orchestration and per-case start/finish events.
- `profile.log`, `profile.json`: calibration progress and raw measurements.
- `metadata.json`: resources, local CPU pinning, local/remote runtime hashes.
- `source/`: frozen script and protocol actually used by this run.
- `r*/*.json`: eight worker measurements and aggregate result per case.
- `r*.log`: individual case execution output.

Launched with nohup, environment activated, native library threads fixed to
one and Ray actors pinned via `CEDAR_RAY_PLACEMENT_RESOURCE=cedar_remote`.
There are 81 full-data measurements after profiling; none of the reduced-data
smoke tests enter these results. All three stage counts passed the final
eight-worker smoke test in `../smoke_20260906_v3`; five unit tests passed,
including Local tuple unpacking compatibility. Version v1 stopped in Local
calibration before any full-data run because Local and Ray have different
tuple calling conventions. The callable now accepts both forms, with unchanged
compute work and output. Its failed logs and frozen source are retained.

No main optimizer, paper text or Chapter 6 experiment artifacts were changed.
After COMPLETE, validate counts and timing stability before using results to
choose between aggregation rules or changing boundary coefficients. See
PROTOCOL.md for limitations and the distinction between throughput evidence
and direct overlap measurement.
