# v3 restart: Ray runtime-package initialization race

v2 completed all 63 calibration entries but failed before the first full-data
measurement. Worker 5 reported FileNotFoundError when Ray's concurrent local
working-directory uploads unlinked the same `_ray_pkg_46315aab85aced88.zip`.
Other workers reported secondary BrokenBarrierError after its failure.

The driver group now shares a multiprocessing lock around ray.init only.
Actor creation, warmup and measurement remain unchanged; all eight drivers
still enter a barrier before simultaneous measured execution. No Cedar runtime
or optimizer was changed. A concurrency regression test exercises exclusive
shared-package creation under eight concurrent initialization calls.

New run: `outputs/model_diagnostics/controlled_20260906_v3`.
Launch is nohup with the same environment, full data, resources, three rounds,
81 cases and timeouts. `--reuse-profile-root` imports v2 calibration only after
checking 63 unique expected entries, >=10-second durations, positive counts,
local/remote runtime file hashes, and identical Work/Records/profile ASTs.
`profile_provenance.json` records the original source and profile SHA-256.
v1/v2 data and all failure logs remain unchanged. The canonical script and
v3 frozen source contain the fix; earlier frozen sources retain their original
versions for reproducibility. All six unit tests passed before restart.

Monitor v3/STATUS and v3/runner.log. COMPLETE means the requested measurement
matrix finished; FAILED identifies a failure requiring inspection.
